import raw from '$constants';

/** Display name shown in the UI (spaced). */
export const APP_NAME = raw.appName;

/** Paneforge / layout persistence id for the shell sidebar. */
export const SIDEBAR_LAYOUT_ID = `${raw.envPrefix.toLowerCase()}-shell-sidebar`;
