version: "2"
servers: []
sql:
    - engine: sqlite
      queries: queries
      schema: migrations
      gen:
        go:
            package: db
            out: internal/db
            emit_json_tags: true
            json_tags_case_style: snake
            emit_pointers_for_null_types: true
